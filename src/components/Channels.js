import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Channels = () => {
  return (
    <View style={{ flex: 4, borderWidth: 2, borderColor: "white" }}>
      <Text>Channels</Text>
    </View>
  );
};

export default Channels;

const styles = StyleSheet.create({});
